// Define a function that takes a name as a parameter and returns a greeting message
function greetByName(name) {
    return "Hello " + name + "!";
}

// Invoke the function with a name of your choice and store the return value in a variable
let greetingAlice = greetByName("Alice");

// Print out the variable containing the greeting
console.log(greetingAlice);
